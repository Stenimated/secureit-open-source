---
import Layout from "@/layouts/admin.astro";
import { Middleware } from "@/util/middeware";
const result = await Middleware(Astro);
if (result) {
    return result;
}
---

<Layout title="Employee/Users" />
